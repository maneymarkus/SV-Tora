<div class="page" data-min-fighters="{{ $fightingSystem->min_fighters ?? 0 }}" data-max-fighters="{{ $fightingSystem->max_fighters ?? 0 }}">
    <h3>{{ $fightingSystem->name ?? "" }}</h3>
    <a class="secondary-button">
        <span class="text">
            Wählen
        </span>
    </a>
</div>
