function init(window, document, undefined) {
  const tilt = $('.host-tournament').tilt({maxTilt: 15, scale: 1.1});
}

window.addEventListener("DOMContentLoaded", function() {
  init(window, document);
});
